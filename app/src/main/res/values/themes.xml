<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window background follows the system theme so launch does not flash the wrong colour. -->
    <style name="Theme.MathOnGoAssignment" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
